export default function InfoPage() {
  return (
    <p>
      Fill in information in the profile sections, then go to resume builder to
      pick which information to add in a resume
    </p>
  );
}
